# NPC / opponent portraits

Place opponent/NPC images here. Filename must match `opponentImagePath` in `frontend/src/data/world.ts` (e.g. `balamb_student.png`, `garden_student.png`). Add images manually or extend `frontend/scripts/download-character-art.mjs` to fetch NPC art.
