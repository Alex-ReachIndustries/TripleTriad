# Character portraits

Place character portrait images here. Filename must match character `id` from `frontend/src/data/characters.ts` (e.g. `zell.png`, `squall.png`).

Run `node frontend/scripts/download-character-art.mjs` from repo root to fetch a subset from the FF wiki; add or replace files manually as needed.
